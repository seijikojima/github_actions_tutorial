name: hoge ci
on: push

on:
  push:
    brances:
      - master
    paths:
      - '*'


jobs:
 build:
  name: hoge
  runs-on: ubuntu-latest
  env:
   USER_NAME: "hoge"
  steps:
  - name : test1
   run: |
    pwd
    ls
    run : cat ../../README.md
    #  - run : echo "secret user name : ${{secrets.USER_NAME}}"
